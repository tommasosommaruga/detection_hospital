# Core (CPU-friendly, works on Google Colab out of the box)
numpy>=1.24
pillow>=10.0
pyyaml>=6.0

# Deep-learning backend. On Colab, torch+CUDA is preinstalled so this is a no-op.
# On a CPU-only machine, pip installs the CPU build automatically.
torch>=2.2

# Plotting (used by the training/results notebook)
matplotlib>=3.7

# Testing
pytest>=8.0

# --- Optional, uncomment when you add real WSI support ---
# torchvision>=0.17
# openslide-python>=1.3  # read .svs / .ndpi whole-slide images (needs system openslide)
# opencv-python>=4.9
